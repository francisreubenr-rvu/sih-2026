# Single-context domain documentation

Read root CONTEXT.md and relevant `Docs/decisions/` ADRs before changing behavior. Use its vocabulary. If a decision conflicts with an ADR, state the conflict and record the replacement with evidence. Missing auxiliary documents do not block authorized work.
