# Wave 5 — e2e maximize + selective redaction utility (14 September 2026)

## Decisions

1. **Toolbar glyph remains human-only.** Automation proves everything else on the synthetic fixture (inject, collect, activeTab refusal, overlay UI loop, screenshots). Harness overlay is temporary and deleted; shipped manifest unchanged.
2. **Text private regions only when classifiers fire.** Unknown text still never becomes an allow-listed control. Selective preview utility improves without authorizing raw export.
3. **`password` is a first-class region kind** in the Zod scene schema (opaque geometry only).
4. **`mergeRegions`** coalesces abutting same-kind boxes to reduce mosaic over-paint.
5. **G11 budget unchanged.** Judge-facing breakdown separates privacy-only local protect from planner-assisted full flow.
6. **Gate honesty.** Evidence files for G05/G09/G14/G20 do not flip status to pass without acceptance criteria. G17/G18 pass on existing deck/README evidence.

## Non-claims

- Not WebPII re-score; external 58/100 retention failure unchanged.
- Not official rubric saturation (score null).
- Not Firefox live validation; not Ollama planner E2E (unreachable).
